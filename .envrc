# Development environment variables for bt
# Use with direnv (https://direnv.net/) or source manually: source .envrc

export BT_CONFIG_PATH="$HOME/.config/bt.toml.dev"
export BT_HOME="$HOME/.local/share/bt-dev"
